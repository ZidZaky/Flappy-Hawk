import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

public class GameOver extends Actor {
    public GameOver() {
        setImage(new GreenfootImage("game-over.png"));  // Ensure you have this image in your images folder
    }

    public void act() {
        // No action needed in act for the game over message
    }
}
